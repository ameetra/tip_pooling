# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.npm
.eslintcache
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.staging
.env.production

# Build outputs
dist/
build/
out/
*.local

# Logs
logs/
*.log
pids/
*.pid
*.seed
*.pid.lock

# Coverage
coverage/
.nyc_output/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform/environments/*/*.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.zip

# AWS
.aws/
*.pem
*.key
*_accessKeys.csv
*.csv

# Prisma
.prisma/
**/generated/prisma-pg/

# Lambda build artifacts
lambda-package/

# Testing
.vitest/

# Misc
.cache/
tmp/
temp/

# Secrets / local-only — never commit
CREDENTIALS.local.md
*.local.md
.claude/settings.local.json

# Local MCP server config (Playwright, etc.) + its output — local tooling, not shared
.mcp.json
.playwright-mcp/

# Operator / planning / security docs — keep local (operational + internal info), not in the public repo
SUPER_ADMIN_GUIDE.md
RUNBOOK_ADD_TENANT.md
IMPLEMENTATION_PLAN.md
SECURITY_REVIEW.md

# Tenant staging data may contain employee PII — keep logos (images), ignore data files
tenants/**/*.csv
tenants/**/*.tsv
tenants/**/*.xlsx
tenants/**/*.xls
tenants/**/*.json
tenants/**/*.txt
tenants/**/*.pdf
